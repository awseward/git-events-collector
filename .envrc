use_nix
asdf install
>&2 cat <<MSG

Unless already done, you may need: brew bundle install --no-upgrade --no-lock

MSG

# I'd love to be able to do this, but it completely circumvents the `direnv
# allow/deny` security functionality...
if [ -f '.envrc.local' ]; then
  source '.envrc.local'
fi
